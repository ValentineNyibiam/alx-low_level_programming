#ifndef MAIN_H
#define MAIN_H

/* standard libraries */
#include <stddef.h>
#include <stdlib.h>

/* prototypes */
char *create_array(unsigned int size, char c);
int _putchar(char c);
char *_strdup(char *str);

#endif /* MAIN_H */
