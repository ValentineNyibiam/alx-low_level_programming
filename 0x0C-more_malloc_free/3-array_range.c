#include "main.h"

/**
 * *array_range - creates an array of integers and intializes it with values
 * @min: the minimum integer value which the array will contain
 * @max: the maximum integer value which the array will contain
 *
 * Return: a pointer to the allocated array which has been initialized
 * else NULL
 */
int *array_range(int min, int max)
{
	/* initialize variables */
	int *alloc_mem;

	/* allocate memory dynamically */
	alloc_mem = 
	/* check if allocation was successful */
	/* intialize allocated memory */
}
